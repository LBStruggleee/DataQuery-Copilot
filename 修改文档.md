# 修改文档

## 1. 整体方向

以"API 与前后端边界理顺"为主线：后端按路由 / 服务 / 领域分层，契约统一并加版本，前端用生成的类型消费接口。性能和独立演进只做最小闭环，不发散。

## 2. 主要功能 / 改动

### 方向 1 — 后端分层 + 契约统一（主线）

- 路由层只做 HTTP：版本前缀 `/api/v1`、请求校验、错误映射。
- 服务层做编排：`DataQueryService` 统一组装 loader / engine / visualizer，去掉各端点各自拼逻辑。
- 统一响应信封和错误码：成功 / 失败同一形状，`detail` 不再是自由文本。
- 契约文档化：OpenAPI 描述与实现同源，作为前后端唯一依据。

### 方向 2（并入两项）— 服务能力补齐

- 分页 / 行数上限：`/api/query` 加 `limit` + 分页参数，大结果不再全量转 records。
- 错误统一：参数错误 / 表不存在 / 执行失败映射到明确状态码，前端可按码展示。

### 方向 3（最小闭环）— 前后端独立演进

- OpenAPI 生成 TS 类型：替换手写 `frontend/src/types.ts`，`mockData.ts` 与契约同源。
- 契约测试：后端 OpenAPI 快照 + 前端 contract test，防止两边漂移。

## 3. 非目标（这次不做）

- 鉴权、限流、多数据源、缓存共享策略、同步改异步。
- 前端大改，只配合契约做小改。

## 4. 验收标准

- `api/models.py` 是唯一契约源，`types.ts` 由生成产物替代。
- 错误响应全局一致，前端能按码处理。
- 大查询有上限 / 分页保护。
- 契约测试覆盖核心接口。
